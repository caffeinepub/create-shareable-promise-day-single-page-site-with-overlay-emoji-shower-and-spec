## Create shareable Promise Day single-page site with overlay emoji shower and specified photo layout

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
